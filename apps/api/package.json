{
  "name": "@repo/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "go build -o tend-api ./cmd/server",
    "lint": "go vet ./...",
    "typecheck": "go build -o /dev/null ./...",
    "test": "go test -timeout 5m ./..."
  }
}
